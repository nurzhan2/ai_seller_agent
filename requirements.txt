pydantic>=2.9
pydantic-settings>=2.5
PyYAML>=6.0
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
tenacity>=9.0
redis>=5.1
SQLAlchemy>=2.0
alembic>=1.14
asyncpg>=0.30
# /metrics (app/metrics.py) — упущен из requirements.txt при первой сборке:
# локально стоял в окружении, поэтому 543 теста были зелёными, а в чистом
# контейнере на Railway импорт падал ModuleNotFoundError ещё на старте.
prometheus_client>=0.20
Pillow>=11.0
# Оба LLM-провайдера (промт №12) идут через этот SDK: DeepSeek отдаёт
# Anthropic-совместимые ответы на своём base_url — см.
# app/agent/providers/deepseek_provider.py.
anthropic>=0.125
# Операторский Telegram-бот, запускается фоновой задачей в lifespan
# приложения (app/main.py, промт №13).
aiogram>=3.30
# Только для тестов SqlAlchemyZoneMapping (tests/test_zone_mapping.py) —
# in-memory SQLite вместо поднятого Postgres. Тесты pytest/respx в этом
# файле намеренно не перечислены (они уже были нужны раньше и не были
# добавлены здесь — не расширяю тот пробел мимо того, что сам же завёл).
aiosqlite>=0.20
